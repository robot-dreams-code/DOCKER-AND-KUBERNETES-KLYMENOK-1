Основні команди
===============

`kubectl apply -f deployment.yaml` - запустити деплой і піднятя подів  
`kubectl get pods` - переглянути статус pods  
`kubectl get rs` - інфоромація про replicaSet  
`kubectl scale deployment course-app --replicas=0` - зупинити всі pods