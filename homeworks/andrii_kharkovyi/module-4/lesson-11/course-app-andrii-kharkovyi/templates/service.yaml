apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-app-andrii-kharkovyi.fullname" . }}
  labels:
    {{- include "course-app-andrii-kharkovyi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-app-andrii-kharkovyi.selectorLabels" . | nindent 4 }}
