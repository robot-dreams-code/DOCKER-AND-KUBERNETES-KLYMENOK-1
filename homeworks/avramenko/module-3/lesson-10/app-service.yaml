apiVersion: v1
kind: Service
metadata:
  namespace: rd
  name: course-app
spec:
  selector:
    app: course-app
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 31000
      targetPort: 8080
  type: NodePort
