apiVersion: v1
kind: Service
metadata:
  name: course-app-service
  labels:
    app: course-app
spec:
  type: NodePort
  selector:
    app: course-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080