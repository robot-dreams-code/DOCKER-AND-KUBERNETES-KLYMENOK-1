apiVersion: v1
kind: Service
metadata:
  name: course-app-external-service
  labels:
    app: course-app
spec:
  selector:
    app: course-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
