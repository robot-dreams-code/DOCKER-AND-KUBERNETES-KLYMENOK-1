apiVersion: v1
kind: Service
metadata:
  name: {{ include "lesson-11.fullname" . }}
  labels:
    {{- include "lesson-11.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "lesson-11.selectorLabels" . | nindent 4 }}
