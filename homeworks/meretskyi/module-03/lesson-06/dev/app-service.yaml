apiVersion: v1
kind: Service
metadata:
  namespace: course-app-dev
  name: course-app-svc
spec:
  type: NodePort
  selector:
    app: course-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      nodePort: 30080 # available ports: 30000–32767
