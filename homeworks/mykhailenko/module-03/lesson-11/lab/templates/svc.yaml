apiVersion: v1
kind: Service
{{ include "lab.metadata" . }}
spec:
  type: ClusterIP
  selector:
    {{ include "lab.labels.list" . | indent 4 }}
  ports:
    - name: api
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}