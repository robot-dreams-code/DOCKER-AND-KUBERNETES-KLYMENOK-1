apiVersion: v1
kind: Service
metadata:
  name: app-svc
  namespace: lesson-07
spec:
  selector:
    app: app
  type: NodePort
  ports:
    - name: app
      port: 8080
      targetPort: 8080
      nodePort: 30087