apiVersion: v1
kind: Service
metadata:
  name: app-svc
  namespace: lesson-08
spec:
  selector:
    app: app
  type: ClusterIP
  ports:
    - name: app
      port: 8080
      targetPort: 8080
      #nodePort: 30088