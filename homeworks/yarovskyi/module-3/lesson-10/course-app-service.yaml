apiVersion: v1
kind: Service
metadata:
  name: course-app
  namespace: robotdreams
spec:
  type: ClusterIP
  selector:
    app: course-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
