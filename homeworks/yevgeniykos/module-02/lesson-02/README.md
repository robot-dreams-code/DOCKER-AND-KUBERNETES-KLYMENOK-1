# DOCKER-AND-KUBERNETES-KLYMENOK-1

## Home work 1

Для запуску 10 контейнерів nginx ми використали скрипт ./run_bash.sh:
 - стандартний порт 8080 заййнятий  іншим застосунком на хості, тому нумераціяя починається з 8081.
 - команда docker run використовується з прапорцем --rm, щоб видаляти контейнери після зупинки
для кожного контейнеру була вибрана унікальна назва, щоб не сплутати тестові контейнери з робочими

![alt text](img/image.png)

для видалення контейнерів ми також використаєм скрипт ./kill_bash.sh:
 - щоб не видялити зайві контейнери ми також робимо фільтр по імені контейнера
 - команду  'docker stop $(docker ps -a -q)' ми не можемо використати, бо видалиться все запущене середовище
