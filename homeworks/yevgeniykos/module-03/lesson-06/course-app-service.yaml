apiVersion: v1
kind: Service
metadata:
  name: course-app-service
  namespace: mod03-less06
  labels:
    app: course-app
spec:
  type: NodePort
  selector:
    app: course-app
  ports:
    - port: 8080 
      targetPort: 8080 
      nodePort: 30888 